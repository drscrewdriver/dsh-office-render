// dsh-office-render — browser half (client module-loader bundle).
// Generated by scripts/build.mjs from src/client/**; edit the sources, not this file.
window.__ModuleLoader__.load({
  id: "dsh-office-render",
  factory: (require) => {
    var module = { exports: {} };
    var exports = module.exports;
"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/client/index.tsx
var client_exports = {};
__export(client_exports, {
  apply: () => apply,
  inject: () => inject
});
module.exports = __toCommonJS(client_exports);
var import_react2 = require("react");

// src/client/OfficePdfViewer.tsx
var import_react = require("react");

// src/client/pdfUrl.ts
var FIT_WIDTH = "#view=FitH";
function pdfViewerSrc(pdfUrl) {
  const withoutFragment = pdfUrl.split("#")[0] ?? pdfUrl;
  return `${withoutFragment}${FIT_WIDTH}`;
}

// src/client/utils.ts
function formatBytes(bytes) {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
}
function basename(path) {
  const parts = path.split(/[\\/]/);
  return parts[parts.length - 1] || path;
}

// src/client/OfficePdfViewer.tsx
var import_jsx_runtime = require("react/jsx-runtime");
function isBytes(value) {
  return value instanceof Uint8Array;
}
function extensionOf(path) {
  const name = basename(path);
  const at = name.lastIndexOf(".");
  return at < 0 ? "" : name.slice(at + 1).toLowerCase();
}
async function failureOf(response) {
  try {
    const body = await response.json();
    if (typeof body.error === "string" && body.error !== "") return body.error;
  } catch {
  }
  return `HTTP ${response.status}`;
}
function OfficePdfViewer({ path, title, customData, t, convertPath, kind, engine }) {
  const fileName = title !== void 0 && title !== "" ? title : basename(path);
  const [url, setUrl] = (0, import_react.useState)(null);
  const [size, setSize] = (0, import_react.useState)(0);
  const [error, setError] = (0, import_react.useState)(null);
  const [attempt, setAttempt] = (0, import_react.useState)(0);
  const retry = (0, import_react.useCallback)(() => {
    setError(null);
    setUrl(null);
    setAttempt((value) => value + 1);
  }, []);
  (0, import_react.useEffect)(() => {
    if (!isBytes(customData)) return;
    const controller = new AbortController();
    let objectUrl = null;
    let cancelled = false;
    void (async () => {
      try {
        const query = new URLSearchParams({ ext: extensionOf(path) });
        const response = await fetch(`${convertPath}?${query.toString()}`, {
          method: "POST",
          // The bytes are the body: the host never re-derives a path from us, so
          // the workspace fence stays exactly where the sidebar put it.
          body: customData,
          headers: { "Content-Type": "application/octet-stream" },
          signal: controller.signal
        });
        if (!response.ok) {
          const message = await failureOf(response);
          if (!cancelled) setError(message);
          return;
        }
        const blob = await response.blob();
        if (cancelled) return;
        objectUrl = URL.createObjectURL(blob);
        setSize(blob.size);
        setUrl(objectUrl);
      } catch (cause) {
        if (cancelled) return;
        setError(cause instanceof Error ? cause.message : String(cause));
      }
    })();
    return () => {
      cancelled = true;
      controller.abort();
      if (objectUrl !== null) URL.revokeObjectURL(objectUrl);
    };
  }, [customData, path, convertPath, attempt]);
  if (!isBytes(customData)) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-root", children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-loading", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-spinner" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: t("state.converting") })
    ] }) });
  }
  if (error !== null) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-root", children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-error", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-error__title", children: [
        "\u274C ",
        t("state.error")
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-error__message", children: error }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", { type: "button", className: "off-retry", onClick: retry, children: t("state.retry") }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-error__hint", children: t("hint.why") }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-error__hint", children: t("hint.fallback") })
    ] }) });
  }
  if (url === null) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-root", children: /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-loading", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-spinner" }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { children: t("state.converting") }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", { className: "off-loading__hint", children: t("state.convertingHint") })
    ] }) });
  }
  return /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-root", children: [
    /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("div", { className: "off-head", children: [
      /* @__PURE__ */ (0, import_jsx_runtime.jsxs)("span", { className: "off-head__name", title: fileName, children: [
        "\u{1F4C4} ",
        fileName
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "off-head__tag", children: t("meta.units", { size: formatBytes(size) }) }),
      engine !== void 0 && /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "off-head__tag", children: t("meta.engine", { engine }) }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", { className: "off-head__badge", children: t("badge") }),
      /* @__PURE__ */ (0, import_jsx_runtime.jsx)("a", { className: "off-head__link", href: pdfViewerSrc(url), target: "_blank", rel: "noreferrer", children: t("state.openPdf") })
    ] }),
    /* @__PURE__ */ (0, import_jsx_runtime.jsx)("iframe", { className: "off-frame", src: pdfViewerSrc(url), title: fileName })
  ] });
}

// src/client/locales.ts
var NS = "dsh-office-render";
var dictionaries = {
  en: {
    "viewer.docx": "Document (faithful)",
    "viewer.pptx": "Presentation (faithful)",
    "badge": "faithful layout \xB7 rendered as PDF",
    "state.converting": "Rendering with the Office suite on this machine\u2026",
    "state.convertingHint": "The first look at a file takes a few seconds; the result is cached afterwards.",
    "state.error": "Could not render this file faithfully",
    "state.retry": "Try again",
    "state.openPdf": "Open the PDF in a new tab",
    "meta.units": "{size} \xB7 PDF",
    "meta.engine": "engine {engine}",
    "hint.why": "This plugin converts the file with an Office-compatible suite installed on the host machine (WPS Office or Microsoft Office) and shows the PDF, so the layout is the real one.",
    "hint.fallback": "Disable dsh-office-render to go back to the structured reading view, which needs no converter."
  },
  zh: {
    "viewer.docx": "\u6587\u6863\uFF08\u7248\u5F0F\u8FD8\u539F\uFF09",
    "viewer.pptx": "\u6F14\u793A\u6587\u7A3F\uFF08\u7248\u5F0F\u8FD8\u539F\uFF09",
    "badge": "\u7248\u5F0F\u8FD8\u539F \xB7 \u4EE5 PDF \u5448\u73B0",
    "state.converting": "\u6B63\u5728\u7528\u672C\u673A Office \u6E32\u67D3\u2026",
    "state.convertingHint": "\u9996\u6B21\u6253\u5F00\u9700\u8981\u51E0\u79D2\uFF0C\u7ED3\u679C\u4F1A\u88AB\u7F13\u5B58\uFF0C\u4E4B\u540E\u7ACB\u5373\u663E\u793A\u3002",
    "state.error": "\u65E0\u6CD5\u5B8C\u6210\u7248\u5F0F\u6E32\u67D3",
    "state.retry": "\u91CD\u8BD5",
    "state.openPdf": "\u5728\u65B0\u6807\u7B7E\u9875\u6253\u5F00 PDF",
    "meta.units": "{size} \xB7 PDF",
    "meta.engine": "\u5F15\u64CE {engine}",
    "hint.why": "\u672C\u63D2\u4EF6\u7528\u5BBF\u4E3B\u673A\u4E0A\u5DF2\u5B89\u88C5\u7684 Office \u517C\u5BB9\u5957\u4EF6\uFF08WPS Office \u6216 Microsoft Office\uFF09\u628A\u6587\u4EF6\u8F6C\u6210 PDF \u518D\u663E\u793A\uFF0C\u6240\u4EE5\u7248\u5F0F\u662F\u771F\u5B9E\u7684\u3002",
    "hint.fallback": "\u505C\u7528 dsh-office-render \u5373\u53EF\u56DE\u5230\u7ED3\u6784\u5316\u9605\u8BFB\u89C6\u56FE \u2014\u2014 \u90A3\u6761\u8DEF\u4E0D\u9700\u8981\u4EFB\u4F55\u8F6C\u6362\u5668\u3002"
  }
};
function interpolate(template, vars) {
  if (vars === void 0) return template;
  return template.replace(/\{(\w+)\}/g, (match, name) => {
    const value = vars[name];
    return value === void 0 ? match : String(value);
  });
}
function translatorFrom(dict) {
  return (key, vars) => interpolate(dict[key] ?? key, vars);
}

// src/client/seams.ts
function isBetterSidebar(value) {
  if (typeof value !== "object" || value === null) return false;
  return typeof value.registerFileViewer === "function";
}
function isLocale(value) {
  if (typeof value !== "object" || value === null) return false;
  const candidate = value;
  return typeof candidate.register === "function" && typeof candidate.bind === "function";
}
function clientContextOf(raw) {
  const ctx = raw ?? {};
  return {
    effect: typeof ctx.effect === "function" ? ctx.effect.bind(raw) : () => {
    },
    ...isBetterSidebar(ctx.betterSidebar) ? { betterSidebar: ctx.betterSidebar } : {},
    ...isLocale(ctx.locale) ? { locale: ctx.locale } : {}
  };
}
function sidebarFileUrl(scope, path) {
  const params = new URLSearchParams({ sessionId: scope.sessionId, path });
  if (scope.cwd !== void 0 && scope.cwd !== "") params.set("cwd", scope.cwd);
  return `/sidebar/file?${params.toString()}`;
}

// src/client/styles.css
var styles_default = "/*\n * dsh-office-render \u2014 stylesheet.\n *\n * Injected at runtime: a DSH plugin has no CSS entry point, so the build inlines\n * this file as a string and `apply` appends one <style> tag inside a\n * `ctx.effect`, whose disposer removes it on unload.\n *\n * Every colour is a DSH design token (`--dsw-alias-*`) with a neutral fallback,\n * so the frame follows the active skin.\n */\n\n.off-root {\n  display: flex;\n  flex-direction: column;\n  height: 100%;\n  min-height: 0;\n  overflow: hidden;\n  font-size: 12px;\n  color: var(--dsw-alias-label-primary, #e6e6e6);\n}\n\n/* \u2500\u2500 Header strip \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n\n.off-head {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  flex-wrap: wrap;\n  padding: 6px 12px;\n  border-bottom: 1px solid var(--dsw-alias-border-l1, #2a2a2a);\n  font-size: 11px;\n  color: var(--dsw-alias-label-secondary, #aaa);\n}\n\n.off-head__name {\n  overflow: hidden;\n  text-overflow: ellipsis;\n  max-width: 220px;\n  color: var(--dsw-alias-label-primary, #e6e6e6);\n}\n\n.off-head__tag {\n  padding: 1px 6px;\n  border-radius: 4px;\n  font-size: 10px;\n  background: var(--dsw-alias-interactive-bg-hover, rgba(255, 255, 255, 0.06));\n}\n\n/* The one thing a reader must not have to guess. */\n.off-head__badge {\n  padding: 1px 6px;\n  border-radius: 4px;\n  font-size: 10px;\n  border: 1px solid var(--dsw-alias-border-l2, #3a3a3a);\n  color: var(--dsw-alias-label-caption, #888);\n}\n\n.off-head__link {\n  margin-left: auto;\n  font-size: 10px;\n  color: var(--dsw-alias-brand-primary, #60a5fa);\n  text-decoration: none;\n}\n\n.off-head__link:hover {\n  text-decoration: underline;\n}\n\n/* \u2500\u2500 The PDF surface \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n\n/* `flex: 1` plus `min-height: 0` is what lets an iframe shrink inside a flex\n   column; without the second one the frame ignores the container height. */\n.off-frame {\n  flex: 1;\n  min-height: 0;\n  width: 100%;\n  border: 0;\n  background: var(--dsw-alias-bg-layer-1, #1e1e1e);\n}\n\n/* \u2500\u2500 Loading / error \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500 */\n\n.off-loading,\n.off-error {\n  flex: 1;\n  display: flex;\n  flex-direction: column;\n  align-items: center;\n  justify-content: center;\n  gap: 8px;\n  padding: 32px 16px;\n  text-align: center;\n  color: var(--dsw-alias-label-tertiary, #888);\n}\n\n.off-error__title {\n  font-size: 13px;\n  color: var(--dsw-alias-state-error-primary, #ef4444);\n}\n\n.off-error__message {\n  font-size: 11px;\n  color: var(--dsw-alias-label-secondary, #aaa);\n  max-width: 320px;\n  overflow-wrap: anywhere;\n}\n\n.off-error__hint {\n  font-size: 11px;\n  color: var(--dsw-alias-label-caption, #777);\n  max-width: 320px;\n}\n\n.off-retry {\n  padding: 4px 14px;\n  border-radius: 6px;\n  border: 1px solid var(--dsw-alias-border-l2, #3a3a3a);\n  background: var(--dsw-alias-bg-layer-3, transparent);\n  color: var(--dsw-alias-label-primary, #e6e6e6);\n  font: inherit;\n  font-size: 11px;\n  cursor: pointer;\n}\n\n.off-retry:hover {\n  background: var(--dsw-alias-interactive-bg-hover, rgba(255, 255, 255, 0.06));\n}\n\n.off-loading__hint {\n  font-size: 11px;\n  color: var(--dsw-alias-label-caption, #777);\n  max-width: 300px;\n}\n\n.off-spinner {\n  width: 22px;\n  height: 22px;\n  border: 2px solid var(--dsw-alias-border-l2, #3a3a3a);\n  border-top-color: var(--dsw-alias-brand-primary, #60a5fa);\n  border-radius: 50%;\n  animation: off-spin 0.8s linear infinite;\n}\n\n@keyframes off-spin {\n  to {\n    transform: rotate(360deg);\n  }\n}\n\n@media (prefers-reduced-motion: reduce) {\n  .off-spinner {\n    animation-duration: 2s;\n  }\n}\n";

// src/client/index.tsx
var BASE = "/office-render";
var TAG = "[dsh-office-render]";
var PRIORITY = 100;
var inject = ["betterSidebar", "locale"];
var STYLE_ID = "dsh-office-render-styles";
var SPECS = {
  docx: { id: "dsh-office-render:docx", titleKey: "viewer.docx", exts: ["docx", "docm", "dotx"] },
  pptx: { id: "dsh-office-render:pptx", titleKey: "viewer.pptx", exts: ["pptx", "pptm", "potx"] }
};
function injectStyles() {
  if (document.getElementById(STYLE_ID) !== null) return () => {
  };
  const tag = document.createElement("style");
  tag.id = STYLE_ID;
  tag.textContent = styles_default;
  document.head.appendChild(tag);
  return () => tag.remove();
}
function translatorOf(ctx) {
  const locale = ctx.locale;
  if (locale === void 0) return translatorFrom(dictionaries.en);
  return (key, vars) => {
    try {
      const bound = locale.bind(NS)(key);
      return interpolate(bound === "" ? key : bound, vars);
    } catch {
      return interpolate(key, vars);
    }
  };
}
var healthTask;
function health() {
  if (healthTask === void 0) {
    healthTask = (async () => {
      try {
        const response = await fetch(`${BASE}/health`);
        if (!response.ok) {
          console.log(`${TAG} host routes answered HTTP ${response.status}; faithful rendering stays off.`);
          return void 0;
        }
        return await response.json();
      } catch (error) {
        console.log(`${TAG} host routes unreachable; faithful rendering stays off.`, error);
        return void 0;
      }
    })();
  }
  return healthTask;
}
function descriptor(kind, spec, healthResponse, t) {
  return {
    id: spec.id,
    title: () => t(spec.titleKey),
    exts: spec.exts,
    priority: PRIORITY,
    fetchStrategy: "custom",
    load: async (path, scope, signal) => {
      const response = await fetch(sidebarFileUrl(scope, path), { signal });
      if (!response.ok) throw new Error(`HTTP ${response.status} while reading ${path}`);
      return new Uint8Array(await response.arrayBuffer());
    },
    component: (props) => (0, import_react2.createElement)(OfficePdfViewer, {
      path: props.path,
      title: props.title,
      customData: props.customData,
      t,
      convertPath: healthResponse.convertPath ?? `${BASE}/convert`,
      kind,
      ...healthResponse.engines[kind] !== void 0 ? { engine: healthResponse.engines[kind] } : {}
    })
  };
}
function apply(rawCtx) {
  const ctx = clientContextOf(rawCtx);
  ctx.effect(injectStyles, `${TAG} stylesheet`);
  if (ctx.locale !== void 0) {
    for (const [tag, dict] of Object.entries(dictionaries)) {
      ctx.effect(() => ctx.locale.register(NS, tag, dict), `${TAG} dictionary ${tag}`);
    }
  }
  const t = translatorOf(ctx);
  const bar = ctx.betterSidebar;
  if (bar === void 0) {
    console.warn(`${TAG} ctx.betterSidebar \u672A\u53D1\u5E03\uFF1Adsh-better-sidebar \u672A\u5B89\u88C5\u6216\u5DF2\u7981\u7528\uFF0C\u7248\u5F0F\u6E32\u67D3\u4FDD\u6301\u60F0\u6027\u3002`);
    return;
  }
  ctx.effect(() => {
    let disposed = false;
    const disposers = [];
    void (async () => {
      const reported = await health();
      if (disposed) return;
      const kinds = reported?.kinds ?? [];
      if (kinds.length === 0) {
        console.log(
          `${TAG} \u672C\u673A\u6CA1\u6709\u53EF\u7528\u7684 Office \u8F6C\u6362\u5668\uFF08WPS / Microsoft Office\uFF09\uFF0C\u4E0D\u6CE8\u518C\u9884\u89C8\u5668\uFF1A.docx/.pptx \u4ECD\u7531\u7ED3\u6784\u5316\u9605\u8BFB\u89C6\u56FE\u63A5\u624B\u3002`
        );
        return;
      }
      for (const kind of kinds) {
        const spec = SPECS[kind];
        if (spec === void 0) continue;
        disposers.push(bar.registerFileViewer(descriptor(kind, spec, reported, t)));
        console.log(`${TAG} \u5DF2\u6CE8\u518C\u7248\u5F0F\u8FD8\u539F\u9884\u89C8\u5668 ${spec.id}\uFF08\u5F15\u64CE ${reported?.engines[kind] ?? "?"}\uFF09`);
      }
    })();
    return () => {
      disposed = true;
      for (const dispose of disposers) dispose();
    };
  }, `${TAG} conditional viewer registration`);
}
    return module.exports;
  },
});
//# sourceMappingURL=client.js.map
